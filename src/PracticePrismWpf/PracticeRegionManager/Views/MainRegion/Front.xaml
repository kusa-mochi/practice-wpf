<UserControl x:Class="PracticeRegionManager.Views.MainRegion.Front"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock Text="Front" />
        <TextBlock Text="{Binding Hoge}" />
        <Button Content="ごーごー" Command="{Binding LogOutCommand}" Margin="0,0,0,40" />
        <Button Content="もーだる" Command="{Binding OpenDetailModal}" />
    </StackPanel>
</UserControl>
